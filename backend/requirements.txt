fastapi
uvicorn[standard]
sqlalchemy>=2.0
alembic
pydantic>=2,<3
pydantic-settings
asyncpg           # postgres driver for 'postgresql+asyncpg'
aiosqlite         # for local sqlite use; harmless on Render
httpx

# label/QR generation used by the /skips flow
qrcode[pil]
reportlab

weasyprint>=61     # Enable PDF

pydantic[email]
